{
  "name": "claude-kanban",
  "version": "0.0.0",
  "private": true,
  "description": "A kanban board for Claude Agent SDK runs (local mode). See README.md and CLAUDE.md.",
  "type": "module",
  "scripts": {
    "//": "Phase-1/task-01 will replace these with the real scripts (typecheck, lint, format, cli).",
    "bootstrap": "echo 'See tasks/phase-1/task-01-init-monorepo.md before running pnpm install.' && exit 1"
  },
  "engines": {
    "node": ">=22"
  }
}

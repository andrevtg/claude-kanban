// Skeleton — to be implemented in phase-1/task-02.
// Read tasks/phase-1/task-02-protocol-types.md before editing.

export type CardStatus = "backlog" | "ready" | "running" | "review" | "done" | "failed";

// TODO(phase-1/task-02): replace with Zod-derived types
export type Card = unknown;
export type Run = unknown;
export type EventLogEntry = unknown;
